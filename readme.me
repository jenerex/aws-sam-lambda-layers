#to run on local run mongo local first via docker first

docker-compose up -d --build


#Create tsc for TS files

npx tsc

#Build Sam
sam build

#run build to run on local

sam local start-api --env-vars env.json --docker-network nodejs-lambda-app_default
